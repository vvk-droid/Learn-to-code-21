#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    
 int s,n,f=1;
    
    scanf("%d",&n);
    
    if(n!=0)
    {
        for(s=1;s<=n;s++)
        {
            f = f * s;
        }
        
        if(n>0)
            printf("%d",f);
        
        else
            printf("ERROR");
    }
    
    else
        printf("1");
    
    
    
       
    return 0;
}
